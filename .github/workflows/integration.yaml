name: Deploying tours and travels to surge

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    name: Deploying to surge
    steps:
      - uses: actions/checkout@v2
      - name: Install Node.js
        uses: actions/setup-node@v2-beta
        with:
          node-version: 16
      - name: Install Surge
        run: npm install -g surge
      - name: Run surge
        run: surge ./ hasnain_sadiq_assignment_three.surge.sh --token ${{ secrets.SURGE_TOKEN }}